{
  "version": 2,
  "buildCommand": "mkdir -p dist && echo '<!-- placeholder; all routes are rewritten to /api/all -->' > dist/index.html",
  "installCommand": "npm install --omit=dev --no-audit --no-fund --ignore-scripts",
  "outputDirectory": "dist",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/all" }
  ],
  "functions": {
    "api/all.js": {
      "memory": 1024,
      "maxDuration": 30,
      "includeFiles": "commentcustomer-developer/**"
    },
    "api/cron/outbox.js": {
      "memory": 512,
      "maxDuration": 60
    },
    "api/cron/maintenance.js": {
      "memory": 512,
      "maxDuration": 60
    }
  },
  "crons": [
    { "path": "/api/cron/outbox",      "schedule": "0 0 * * *" },
    { "path": "/api/cron/maintenance", "schedule": "0 0 * * *" }
  ]
}
